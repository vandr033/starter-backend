-- This migration was created accidentally from an invalid name prompt
-- ("npx prisma generate"). It is intentionally a no-op to preserve
-- migration history consistency without applying unintended schema changes.
SELECT 1;
